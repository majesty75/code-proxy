# Copy to .env and adjust. Used by docker-compose.

# Path to UTA's SQLite, mounted read-only into decode_service for trname/fwname/start.
# Leave empty to run in demo mode (runtime facts taken from the event instead).
UTA_SQLITE=
